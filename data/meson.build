install_data('piclone.ui', install_dir: resource_dir)

i18n.merge_file(input: 'piclone.desktop.in',
  output: 'piclone.desktop',
  type: 'desktop',
  po_dir: '../po',
  install: true,
  install_dir: desktop_dir
)
